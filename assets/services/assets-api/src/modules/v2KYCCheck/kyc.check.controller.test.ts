import { KycCheckController } from './kyc.check.controller';

describe('KycCheckController', () => {
  let controller: KycCheckController;

  beforeEach(() => {
    controller = new KycCheckController();
  });

  it('KycCheck', async () => {
    await expect(true).toBe(true);
  });
});
