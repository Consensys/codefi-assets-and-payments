import { defineMessages } from 'react-intl';

export const pageLoaderTexts = defineMessages({
  loading: {
    id: 'assets.pageLoaderTexts.loading',
    description: 'Loading text',
    defaultMessage: 'Loading...',
  },
});
