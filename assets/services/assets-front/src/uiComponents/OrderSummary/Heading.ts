import { typography } from 'constants/styles';
import styled from 'styled-components';

export const OrderSummaryHeading = styled.h3`
  font-weight: ${typography.weightMedium};
  font-size: ${typography.sizeF3};
  line-height: 150%;
`;
